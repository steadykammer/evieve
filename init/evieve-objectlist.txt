max oblist "evieve Objects" evi.cfsmooth~;
max oblist "evieve Objects" evi.expsmooth~;
max oblist "evieve Objects" evi.lagsmooth~;
max oblist "evieve Objects" evi.logahr~;
max oblist "evieve Objects" evi.logattackdecay~;
max oblist "evieve Objects" evi.logdecay~;
max oblist "evieve Objects" evi.logsmooth~;
max oblist "evieve Objects" evi.pan~;
max oblist "evieve Objects" evi.quadosc~;
max oblist "evieve Objects" evi.vactrol~;
max oblist "evieve Objects" evi.xfade~;
max oblist "evieve Gen Abstractions" evi.4wave~;
max oblist "evieve Gen Abstractions" evi.allpass~;
max oblist "evieve Gen Abstractions" evi.am~;
max oblist "evieve Gen Abstractions" evi.arpnotch~;
max oblist "evieve Gen Abstractions" evi.audiodust~;
max oblist "evieve Gen Abstractions" evi.baxandall~;
max oblist "evieve Gen Abstractions" evi.biquad.allpass~;
max oblist "evieve Gen Abstractions" evi.biquad.analog~;
max oblist "evieve Gen Abstractions" evi.biquad.bandpass~;
max oblist "evieve Gen Abstractions" evi.biquad.butterworth~;
max oblist "evieve Gen Abstractions" evi.biquadcoeff~;
max oblist "evieve Gen Abstractions" evi.biquad.eqfilter~;
max oblist "evieve Gen Abstractions" evi.biquad.filter~;
max oblist "evieve Gen Abstractions" evi.biquad.highpass~;
max oblist "evieve Gen Abstractions" evi.biquad.highshelf~;
max oblist "evieve Gen Abstractions" evi.biquad.lowpass~;
max oblist "evieve Gen Abstractions" evi.biquad.lowshelf~;
max oblist "evieve Gen Abstractions" evi.biquad.notch~;
max oblist "evieve Gen Abstractions" evi.biquad.peaknotch~;
max oblist "evieve Gen Abstractions" evi.biquad.resonant~;
max oblist "evieve Gen Abstractions" evi.biquad~;
max oblist "evieve Gen Abstractions" evi.blitwaves~;
max oblist "evieve Gen Abstractions" evi.butterworthlp6~;
max oblist "evieve Gen Abstractions" evi.bp~;
max oblist "evieve Gen Abstractions" evi.butterworthlp8~;
max oblist "evieve Gen Abstractions" evi.chorus.digital~;
max oblist "evieve Gen Abstractions" evi.classic.osc~;
max oblist "evieve Gen Abstractions" evi.clip~;
max oblist "evieve Gen Abstractions" evi.clipasym~;
max oblist "evieve Gen Abstractions" evi.comb~;
max oblist "evieve Gen Abstractions" evi.cross~;
max oblist "evieve Gen Abstractions" evi.crossover.3band~;
max oblist "evieve Gen Abstractions" evi.crossover.allpass~;
max oblist "evieve Gen Abstractions" evi.crossover.sum~;
max oblist "evieve Gen Abstractions" evi.crossover~;
max oblist "evieve Gen Abstractions" evi.damp.macro~;
max oblist "evieve Gen Abstractions" evi.damp~;
max oblist "evieve Gen Abstractions" evi.detune~;
max oblist "evieve Gen Abstractions" evi.eq3stereo~;
max oblist "evieve Gen Abstractions" evi.eq3~;
max oblist "evieve Gen Abstractions" evi.eq5~;
max oblist "evieve Gen Abstractions" evi.eq8~;
max oblist "evieve Gen Abstractions" evi.filter.allpass~;
max oblist "evieve Gen Abstractions" evi.filter.bandpass~;
max oblist "evieve Gen Abstractions" evi.filter.bell~;
max oblist "evieve Gen Abstractions" evi.filter.eq~;
max oblist "evieve Gen Abstractions" evi.filter.highpass~;
max oblist "evieve Gen Abstractions" evi.filter.highshelf~;
max oblist "evieve Gen Abstractions" evi.filter.lowpass~;
max oblist "evieve Gen Abstractions" evi.filter.lowshelf~;
max oblist "evieve Gen Abstractions" evi.filter.morph~;
max oblist "evieve Gen Abstractions" evi.filter.notch~;
max oblist "evieve Gen Abstractions" evi.filter.peak~;
max oblist "evieve Gen Abstractions" evi.filter.resonant~;
max oblist "evieve Gen Abstractions" evi.filter.shelving~;
max oblist "evieve Gen Abstractions" evi.filter.tilt~;
max oblist "evieve Gen Abstractions" evi.filter~;
max oblist "evieve Gen Abstractions" evi.flanger.digital~;
max oblist "evieve Gen Abstractions" evi.formletfilter~;
max oblist "evieve Gen Abstractions" evi.freqshift.weaver~;
max oblist "evieve Gen Abstractions" evi.gaussiannoise~;
max oblist "evieve Gen Abstractions" evi.hardclip~;
max oblist "evieve Gen Abstractions" evi.korgms10~;
max oblist "evieve Gen Abstractions" evi.korgms20.select~;
max oblist "evieve Gen Abstractions" evi.korgms20~;
max oblist "evieve Gen Abstractions" evi.ladder.diode~;
max oblist "evieve Gen Abstractions" evi.ladder.moog~;
max oblist "evieve Gen Abstractions" evi.ladder.poles~;
max oblist "evieve Gen Abstractions" evi.ladder.transistor~;
max oblist "evieve Gen Abstractions" evi.limiterfast~;
max oblist "evieve Gen Abstractions" evi.wavefolder.cascade~;
max oblist "evieve Gen Abstractions" evi.wavefolder~;
max oblist "evieve Gen Abstractions" evi.lores~;
max oblist "evieve Gen Abstractions" evi.lorez~;
max oblist "evieve Gen Abstractions" evi.modalres~;
max oblist "evieve Gen Abstractions" evi.modosc~;
max oblist "evieve Gen Abstractions" evi.ladder.antti~;
max oblist "evieve Gen Abstractions" evi.morph.trap~;
max oblist "evieve Gen Abstractions" evi.morph.tri~;
max oblist "evieve Gen Abstractions" evi.multimode~;
max oblist "evieve Gen Abstractions" evi.onepole.shelving~;
max oblist "evieve Gen Abstractions" evi.onepole~;
max oblist "evieve Gen Abstractions" evi.overdrive.atan~;
max oblist "evieve Gen Abstractions" evi.overdrive.sqrt~;
max oblist "evieve Gen Abstractions" evi.overdrive.tanh~;
max oblist "evieve Gen Abstractions" evi.paf~;
max oblist "evieve Gen Abstractions" evi.phaser.analog~;
max oblist "evieve Gen Abstractions" evi.phaseshift~;
max oblist "evieve Gen Abstractions" evi.phasor0~;
max oblist "evieve Gen Abstractions" evi.ramp0~;
max oblist "evieve Gen Abstractions" evi.randy~;
max oblist "evieve Gen Abstractions" evi.rcfilter~;
max oblist "evieve Gen Abstractions" evi.rect.select~;
max oblist "evieve Gen Abstractions" evi.rect~;
max oblist "evieve Gen Abstractions" evi.reson~;
max oblist "evieve Gen Abstractions" evi.ringmod~;
max oblist "evieve Gen Abstractions" evi.saw.select~;
max oblist "evieve Gen Abstractions" evi.saw~;
max oblist "evieve Gen Abstractions" evi.shaper~;
max oblist "evieve Gen Abstractions" evi.sine.select~;
max oblist "evieve Gen Abstractions" evi.sinfold~;
max oblist "evieve Gen Abstractions" evi.skfinputmix~;
max oblist "evieve Gen Abstractions" evi.softclip~;
max oblist "evieve Gen Abstractions" evi.squine.simple~;
max oblist "evieve Gen Abstractions" evi.svf~;
max oblist "evieve Gen Abstractions" evi.svfinputmix~;
max oblist "evieve Gen Abstractions" evi.svfsmooth~;
max oblist "evieve Gen Abstractions" evi.svfsynth~;
max oblist "evieve Gen Abstractions" evi.teeth~;
max oblist "evieve Gen Abstractions" evi.tone.macro~;
max oblist "evieve Gen Abstractions" evi.tremolo~;
max oblist "evieve Gen Abstractions" evi.tri.select~;
max oblist "evieve Gen Abstractions" evi.tri~;
max oblist "evieve Gen Abstractions" evi.vactrol.shape~;
max oblist "evieve Gen Abstractions" evi.vcf~;
max oblist "evieve Gen Abstractions" evi.velvetnoise~;
max oblist "evieve Gen Abstractions" evi.vosim.basic~;
max oblist "evieve Gen Abstractions" evi.zeroxcount~;
max oblist "evieve Gen Abstractions" evi.zeroxoscnoise~;
max oblist "evieve Abstractions" evi.adsr~;
max oblist "evieve Abstractions" evi.algoverb~;
max oblist "evieve Abstractions" evi.atodb;
max oblist "evieve Abstractions" evi.backverb~;
max oblist "evieve Abstractions" evi.biquadcoeff;
max oblist "evieve Abstractions" evi.dbtoa;
max oblist "evieve Abstractions" evi.dcblock~;
max oblist "evieve Abstractions" evi.dfm~;
max oblist "evieve Abstractions" evi.enable;
max oblist "evieve Abstractions" evi.findhost;
max oblist "evieve Abstractions" evi.formantfilter.morph~;
max oblist "evieve Abstractions" evi.formantfilter~;
max oblist "evieve Abstractions" evi.formantosc~;
max oblist "evieve Abstractions" evi.freezeverb~;
max oblist "evieve Abstractions" evi.freqshift.hilbert~;
max oblist "evieve Abstractions" evi.gigaverb~;
max oblist "evieve Abstractions" evi.hilbert~;
max oblist "evieve Abstractions" evi.list.interp;
max oblist "evieve Abstractions" evi.list.xfade;
max oblist "evieve Abstractions" evi.list.product;
max oblist "evieve Abstractions" evi.lpg~;
max oblist "evieve Abstractions" evi.magnitude;
max oblist "evieve Abstractions" evi.mattaverb~;
max oblist "evieve Abstractions" evi.notes2ints;
max oblist "evieve Abstractions" evi.pitchdetect~;
max oblist "evieve Abstractions" evi.plateverb~;
max oblist "evieve Abstractions" evi.polyphaseiirhilbertcoeffs;
max oblist "evieve Abstractions" evi.recursivefolder;
max oblist "evieve Abstractions" evi.ringmod.diode~;
max oblist "evieve Abstractions" evi.sustainverb~;
max oblist "evieve Abstractions" evi.tpqm;
max oblist "evieve Abstractions" evi.transratio;
max oblist "evieve Abstractions" evi.voiceexclude;
max oblist "evieve Abstractions" evi.warmer~;
max oblist "evieve Abstractions" evi.weightingcoeffs;
max oblist "evieve Abstractions" evi.zitaverb~;
max oblist "evieve Abstractions" evi.zpk;
max oblist "evieve Abstractions" mc.evi.bands~;
max oblist "evieve Abstractions" mc.evi.dcblock~;
max oblist "evieve Abstractions" mc.evi.listgen;
max oblist "evieve Abstractions" mc.evi.warmer~;
max oblist "evieve Abstractions" mcs.evi.algoverb~;
max oblist "evieve Abstractions" mcs.evi.backverb~;
max oblist "evieve Abstractions" mcs.evi.freezeverb~;
max oblist "evieve Abstractions" mcs.evi.gigaverb~;
max oblist "evieve Abstractions" mcs.evi.mattaverb~;
max oblist "evieve Abstractions" mcs.evi.plateverb~;
max oblist "evieve Abstractions" mcs.evi.sustainverb~;
max oblist "evieve Abstractions" mcs.evi.zitaverb~;
