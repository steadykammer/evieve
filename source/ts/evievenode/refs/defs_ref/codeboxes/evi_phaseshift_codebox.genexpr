
// identical to [evi.filter.allpass~], almost identical to
// [evi.biquad.allpass~] and MSP [phaseshift~] but using a TPT filter.

require("evi_svfs.genexpr");

Param   smooth(0, min=0, max=333);

                            // In   Hz    Q    ms
out1    = eviFilterAllpassNorm(in1, in2, in3, smooth=smooth);

