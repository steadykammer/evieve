
// evi_softclip.gendsp by pete for evieve, cheap tanh()-like soft clipping, anti-aliased with a simple second-order Adaa

// For more options, greater bandwidth and high-gain anti-aliasing, @see: "evi_clipper.gendsp" / [evi.clipper~] (more expensive).
// For hard clipping only @see: "evi_hardclip.gendsp" / [evi.hardclip~].

// see also: 'evi_adaa.genexpr'
require("evi_clipping.genexpr");

out1 = adSc2(in1, 1, 0);


