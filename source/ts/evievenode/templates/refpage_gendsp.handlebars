<?xml version='1.0' encoding='UTF-8' standalone='yes'?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>
<!--This is an automatically generated file. DO NOT EDIT THIS FILE DIRECTLY.-->
<c74object name='{{object.name}}' module='gen-evi' category='{{object.category}}'> <!-- kind='genobject_dsp'> -->
    <digest>
        evieve Gen Object. {{{object.digest}}} <br />
    </digest>
    <description>
        <b>{{object.name}}</b> is an abstraction for using inside <o>gen~</o> or <b>GenExpr</b>. <br />
        {{#if includes}} <br />
        {{object.name}}.gendsp requires the GenExpr file(s): <br />
        {{#each includes}}<o>{{this}}</o>.genexpr{{#unless @last}}, {{/unless}}{{/each}} <br />
        {{/if}}
        {{#if object.child}} <br />
        {{object.name}} is also available in MSP as: <o>{{object.child}}</o>. <br />
        {{/if}} <br />
	    {{{object.description}}} <br />
    </description>
    <methodlist />
    <constructorlist>
        <constructor name='1'>
            <digest>
                { arguments={}, inlets={ {{constructor.inlets}} } }
            </digest>
        </constructor>
    </constructorlist>
    <geninletlist>
        {{#each inlets}}
        <geninlet name='{{name}}' type='{{type}}' optional='{{optional}}'>
            <digest>
                {{#if digestauto}}{{digestauto}}. {{/if}}{{digest}}
            </digest>
        </geninlet>
        {{/each}}
    </geninletlist>
    <attributelist>
        {{#each attributes}}
        <attribute name='{{name}}' type='{{type}}' get='0' set='1'>
            <digest>
                {{digest}}
            </digest>
            <description>
                {{description}}
            </description>
            <attributelist>
                <attribute name='default' type='{{default.type}}' value='{{default.value}}' />
            </attributelist>
        </attribute>
        {{/each}}
    </attributelist>
    <!-- metadata not part of real genobject ref pages but needed to trick Max -->
    <metadatalist>
        <metadata name="author">Pete Dowling</metadata>
        <metadata name="tag">GenDsp</metadata>
    </metadatalist>
    <seealsolist>
        {{#each seealso}}
        <seealso name='{{name}}' display='{{display}}' type='genrefpage' />
        {{/each}}
    </seealsolist>
</c74object>
