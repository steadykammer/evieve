<?xml version='1.0' encoding='UTF-8' standalone='yes'?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>
<!--This is an automatically generated file. DO NOT EDIT THIS FILE DIRECTLY.-->
<c74object name='{{object.name}}' module='evi-dsp' category='{{object.category}}' kind='genobject_dsp'>
    <digest>
	    {{object.digest}}
    </digest>
    <description>
	    {{object.description}}
    </description>
    <methodlist />
    <constructorlist>
        <constructor name='1'>
            <digest>
                { arguments={}, inlets={ {{constructor.inlets}} } }
            </digest>
        </constructor>
    </constructorlist>
    <geninletlist>
        {{#each inlets}}
        <geninlet name='{{name}}' type='{{type}}' optional='{{optional}}'>
            <digest>
                {{digest}}
            </digest>
        </geninlet>
        {{/each}}
    </geninletlist>
    <attributelist>
        {{#each attributes}}
        <attribute name='{{name}}' type='{{type}}' get='0' set='1'>
            <digest>
                {{digest}}
            </digest>
            <description>
                {{description}}
            </description>
            <attributelist>
                <attribute name='default' type='{{default.type}}' value='{{default.value}}' />
            </attributelist>
        </attribute>
        {{/each}}
    </attributelist>
    <seealsolist>
        {{#each seealso}}
        <seealso name='{{name}}' display='{{display}}' type='genrefpage' />
        {{/each}}
    </seealsolist>
</c74object>
