<?xml version='1.0' encoding='UTF-8' standalone='yes'?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>
<!--This is an automatically generated file. DO NOT EDIT THIS FILE DIRECTLY.-->
<c74object name="{{name}}" module="evi-genexpr" category="{{category}}" kind="genexpr_dsp">
  <digest>
    {{name}} is a .genexpr file and is part of the "evieve" Package. <br />
    It is for using in a <o>codebox</o> or in another <m>.genexpr</m> file via the "require()" command. <br />
    By declaring <m>require("{{name}}");</m> at the top of a codebox you can then access all of the functions included in the {{name}} file. <br />
    {{#if require}}
    The {{name}} file also requires:
    {{#each requires}}
    {{this}} <br />
    {{/each}}
    {{/if}}
  </digest>
  <description>
	{{name}}: {{{description}}} <br />
  </description>
  <misc name="Functions">
    {{#each functions}}
    {{#if document}}
    <entry name="{{name}}()">
      <description>
        {{{digest}}} <br />

        {{#each inputs}}
        In {{id}}: type={{kind}}{{#if default}} (optional){{/if}}: {{name}}, {{#if default}}default: {{default}}, {{/if}}{{digest}} <br />
        {{/each}}

        {{#each returns}}
        Return {{id}}: {{name}}, {{digest}} <br />
        {{/each}}

      </description>
    </entry>
    {{/if}}
    {{/each}}
  </misc>
  <metadatalist>
    <metadata name="author">Pete Dowling</metadata>
    <metadata name="tag">evieve</metadata>
    <metadata name="tag">evieve GenExpr</metadata>
  </metadatalist>
  <seealsolist>
    {{#each seealso}}
    <seealso name="{{this}}" type="genrefpage"/>
    {{/each}}
  </seealsolist>
  <misc name="Discussion">
    <entry name="Keywords">
      <description>evieve {{category}}</description>
    </entry>
  </misc>
</c74object>
